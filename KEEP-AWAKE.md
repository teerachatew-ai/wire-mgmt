# ทำให้เว็บเข้าได้ทันที ไม่ต้องรอ 30-50 วิ (ฟรี)

## ทำไมต้องรอ

Render แพลนฟรีจะ **"หลับ" เมื่อไม่มีคนเข้าเว็บนาน 15 นาที**
พอมีคนเปิดเว็บครั้งถัดไป เครื่องต้องตื่นใหม่ = รอ 30-50 วินาที

**วิธีแก้:** ให้มีตัวยิงเข้าเว็บทุก 10 นาทีในช่วงเวลาทำงาน → เครื่องไม่หลับ → เปิดปุ๊บเข้าได้ปั๊บ

---

## ⚠️ ทำไมต้องตั้งเป็น "เฉพาะช่วงเวลาทำงาน" ไม่ใช่ 24 ชม.

Render ให้ใช้ฟรี **750 ชั่วโมง/เดือน** (นับรวมทุก service ในบัญชี)

| แบบ | ชั่วโมงที่ใช้/เดือน | ผล |
|---|---|---|
| ปลุกตลอด 24 ชม. | ~730 ชม. | ⚠️ เกือบเต็มโควต้า ไม่เหลือเผื่อเลย |
| **ปลุก 06:00–21:00 (15 ชม./วัน)** | **~450 ชม.** | ✅ **ปลอดภัย เหลือเผื่ออีกเยอะ** |
| ปลุก 08:00–18:00 (10 ชม./วัน) | ~300 ชม. | ✅ ปลอดภัยมาก |

กลางคืนไม่มีใครใช้อยู่แล้ว ปล่อยให้หลับไปจะดีกว่า — **ประหยัดโควต้าไว้ และยังฟรีอยู่**

---

# วิธีตั้ง (ใช้ cron-job.org — ฟรี ไม่ต้องผูกบัตร ไม่ต้องเขียนโค้ด)

## ขั้นที่ 1 — สมัคร

เปิด <https://cron-job.org> → กด **Sign up** → ยืนยันอีเมล → เข้าสู่ระบบ

## ขั้นที่ 2 — สร้างงานตั้งเวลา

กด **Create cronjob** แล้วกรอกตามนี้:

| ช่อง | ใส่ว่า |
|---|---|
| **Title** | `ปลุกระบบตัดสายไฟ` |
| **URL** | `https://<ชื่อเว็บของคุณ>.onrender.com/api/ping` |
| **Execution schedule** | เลือก **Custom** |

> 📌 URL เอามาจากหน้าเว็บที่ใช้อยู่ปกติ แล้วต่อท้ายด้วย `/api/ping`
> เช่น ถ้าเข้าเว็บที่ `https://wire-mgmt.onrender.com`
> ให้ใส่ `https://wire-mgmt.onrender.com/api/ping`

## ขั้นที่ 3 — ตั้งเวลา (สำคัญที่สุด)

ในส่วน **Custom schedule** ตั้งแบบนี้:

- **Minutes** → เลือก `0`, `10`, `20`, `30`, `40`, `50` (ยิงทุก 10 นาที)
- **Hours** → เลือกเฉพาะ `6` ถึง `21` (คือ 06:00–21:00 น.)
- **Days of month** → เลือก **every**
- **Months** → เลือก **every**
- **Days of week** → เลือก **every**

> ⏰ ตรวจด้วยว่าใน **Settings → Timezone** ตั้งเป็น **Asia/Bangkok**
> ไม่งั้นเวลาจะเพี้ยนไป 7 ชั่วโมง

## ขั้นที่ 4 — บันทึกและทดสอบ

กด **Create** → แล้วกดปุ่ม **Test run**

ถ้าถูกต้องจะได้ผลลัพธ์ **200 OK** พร้อมข้อความประมาณนี้:

```
{"ok":true,"write_blocked":false,"at":"2026-09-04T03:03:11.171Z"}
```

✅ เสร็จแล้ว — ตั้งแต่ 6 โมงเช้าถึง 3 ทุ่ม เปิดเว็บได้ทันทีไม่ต้องรอ

---

## ตรวจว่ามันทำงานอยู่จริงไหม

ในหน้า cron-job.org กดที่ชื่องาน → ดูแท็บ **History**
ควรเห็นรายการสีเขียว (200) เรียงทุก 10 นาทีในช่วงเวลาที่ตั้งไว้

ถ้าเห็นสีแดง แปลว่ายิงไม่เข้า → เช็ค URL ว่าพิมพ์ถูกไหม (ต้องมี `/api/ping` ต่อท้าย)

---

## หมายเหตุ

- `/api/ping` เป็น endpoint ที่ทำขึ้นมาเพื่อการนี้โดยเฉพาะ — **ไม่แตะฐานข้อมูล** ตอบกลับสั้นมาก
  (ทดสอบแล้วใช้เวลา 0.015 วินาที) จึงแทบไม่กินทรัพยากรและไม่กวนการทำงานปกติ
- ค่า `write_blocked` ที่เห็นในผลลัพธ์: ถ้าขึ้น `true` เมื่อไหร่ แปลว่ามีระบบ 2 ชุดกำลังเขียน
  ฐานข้อมูลเดียวกันอยู่ (ดูรายละเอียดใน `DEPLOY-FLY.md`) — ปกติต้องเป็น `false` เสมอ
- ช่วงที่หลับ (กลางคืน) ถ้ามีคนเข้าเว็บก็ยังเข้าได้ แค่ต้องรอตื่น 30-50 วิ เหมือนเดิม
