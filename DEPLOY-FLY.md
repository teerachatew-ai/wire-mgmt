# โฮสต์สำรอง (Fly.io) — คู่มือแบบทำตามทีละขั้น

Render = ตัวหลัก ใช้งานปกติ · Fly.io = ตัวสำรอง **ปิดไว้ตลอด** เปิดเฉพาะตอน Render ล่ม

---

## ⚠️ กฎเหล็กข้อเดียวที่ต้องจำ

**ห้ามเปิดพร้อมกัน 2 ที่**

ฐานข้อมูลถูกเก็บเป็นไฟล์**ก้อนเดียว** แล้วเขียนทับ**ทั้งก้อน**ทุกครั้งที่มีการแก้ข้อมูล
ถ้า Render กับ Fly เปิดพร้อมกัน ต่างฝ่ายต่างถือสำเนาของตัวเองแล้วเขียนทับกัน → **ข้อมูลหายทั้งฐาน**

ระบบมีตัวกันไว้แล้ว (ทดสอบผ่าน 20 เคส): ถ้าเผลอเปิดพร้อมกัน ฝั่งที่มาทีหลังจะ
**หยุดบันทึกและขึ้น error** แทนการทับข้อมูลของอีกฝั่งทิ้ง — ข้อมูลเดิมจะไม่หาย
แต่งานที่กรอกในฝั่งที่ถูกบล็อกจะไม่ถูกบันทึก **ฉะนั้นยังต้องปิดฝั่งที่ไม่ใช้เสมอ**

---

## ✅ ส่วนที่ติดตั้งให้แล้ว (ไม่ต้องทำ)

- `flyctl` เวอร์ชัน 0.4.97 ติดตั้งแล้วผ่าน winget
- `fly.toml` (ไฟล์ตั้งค่า) อยู่ในโปรเจกต์แล้ว ตั้งเป็นโหมดสำรอง ไม่ตื่นเอง
- ตัวกันข้อมูลทับ อยู่ในโค้ดแล้ว

**ข้อควรรู้ 2 อย่าง:**
1. คำสั่งคือ **`flyctl`** (ไม่ใช่ `fly`)
2. ต้อง **เปิดหน้าต่าง PowerShell ใหม่** ก่อน คำสั่งถึงจะใช้ได้ (ตัวติดตั้งเพิ่ง
   เพิ่ม PATH หน้าต่างเดิมที่เปิดค้างอยู่จะยังไม่รู้จัก)

---

## 📋 สิ่งที่ต้องเตรียม

- อีเมล สำหรับสมัคร Fly.io
- **บัตรเครดิต/เดบิต** — Fly.io บังคับผูกบัตรก่อน deploy ได้ (ไม่ได้ตัดเงินถ้าไม่ได้เปิดเครื่องใช้
  เพราะเราตั้งให้ปิดไว้ตลอด เสียแค่ค่าเก็บ image เดือนละไม่กี่บาท)

---

# ขั้นตอนติดตั้ง (ทำครั้งเดียว)

## ขั้นที่ 1 — เปิด PowerShell ใหม่ แล้วเข้าโฟลเดอร์โปรเจกต์

กด `Win + X` → เลือก **Terminal** หรือ **Windows PowerShell** (ต้องเป็นหน้าต่างใหม่)

```bash
cd "D:\Claude Code\wire-mgmt"
```

เช็คว่า flyctl ใช้ได้แล้ว — ต้องขึ้นเลขเวอร์ชัน `flyctl.exe v0.4.97 ...`

```bash
flyctl version
```

> ❗ ถ้าขึ้น *"ไม่รู้จักคำสั่ง flyctl"* แปลว่ายังใช้หน้าต่างเก่าอยู่ → ปิดแล้วเปิดใหม่

---

## ขั้นที่ 2 — สมัคร / เข้าสู่ระบบ Fly.io

```bash
flyctl auth login
```

**จะเกิดอะไรขึ้น:** เบราว์เซอร์จะเปิดขึ้นมาเอง
1. กด **Sign up** (ถ้ายังไม่มีบัญชี) — เลือกสมัครด้วย Google หรือ GitHub จะเร็วที่สุด
2. เว็บจะขอ **ใส่บัตรเครดิต** → ใส่ให้เรียบร้อย (ขั้นนี้ผมทำแทนไม่ได้)
3. เสร็จแล้วกลับมาที่หน้าต่าง PowerShell จะขึ้นว่า `successfully logged in`

เช็คว่า login ติดแล้ว (ต้องขึ้นอีเมลของคุณ)

```bash
flyctl auth whoami
```

---

## ขั้นที่ 3 — สร้างแอปบน Fly

```bash
flyctl apps create wire-mgmt
```

**ถ้าขึ้นว่าชื่อซ้ำ** (`Name has already been taken`) แปลว่ามีคนอื่นใช้ชื่อนี้ไปแล้ว
ให้ตั้งชื่อใหม่ เช่น `wire-mgmt-kokmuang` :

```bash
flyctl apps create wire-mgmt-kokmuang
```

จากนั้น **ต้องแก้ไฟล์ `fly.toml` บรรทัดแรก** ให้ตรงกับชื่อใหม่ด้วย:
เปลี่ยน `app = "wire-mgmt"` → `app = "wire-mgmt-kokmuang"`
(บอกผมได้ เดี๋ยวแก้ให้)

---

## ขั้นที่ 4 — ใส่ค่าเชื่อมต่อฐานข้อมูล

ใช้ฐานข้อมูลตัวเดียวกับ Render ข้อมูลจะได้ต่อเนื่องกัน
คำสั่งนี้อ่านค่าจากไฟล์ `cloud-db-url.txt` ให้เอง **ไม่ต้องพิมพ์รหัสเอง**

```bash
$env:PGURL = (Get-Content ".\cloud-db-url.txt" -Raw).Trim(); flyctl secrets set "DATABASE_URL=$env:PGURL"
```

ถ้าใช้ผู้ช่วย AI ในระบบด้วย ใส่ key เพิ่ม (ถ้าไม่ได้ใช้ ข้ามได้):

```bash
flyctl secrets set "ANTHROPIC_API_KEY=<วาง key ตรงนี้>"
```

เช็คว่าใส่ครบ (จะโชว์แค่ชื่อ ไม่โชว์ค่า)

```bash
flyctl secrets list
```

---

## ขั้นที่ 5 — build + deploy ครั้งแรก

> ⏱️ ครั้งแรกใช้เวลา **10–20 นาที** เพราะต้องติดตั้ง LibreOffice + Python ลงใน image
> ห้ามปิดหน้าต่างระหว่างรอ

**สำคัญ: ต้องใส่ `--ha=false`** ไม่งั้น Fly จะสร้างเครื่อง 2 เครื่องทำงานพร้อมกัน = ข้อมูลชนกัน
และ `--remote-only` เพราะเครื่องนี้ไม่ได้ติดตั้ง Docker (ให้ build บนเซิร์ฟเวอร์ Fly แทน)

```bash
flyctl deploy --remote-only --ha=false
```

เสร็จแล้วจะขึ้นลิงก์ประมาณ `https://wire-mgmt.fly.dev`

---

## ขั้นที่ 6 — ปิดเครื่องสำรองทิ้งไว้ (สำคัญ!)

deploy เสร็จเครื่องจะยังเปิดอยู่ **ต้องปิดทันที** เพราะ Render กำลังทำงานอยู่

ดูรายการเครื่องก่อน — ให้ **จดรหัสเครื่อง (ID)** ไว้ เป็นชุดตัวอักษร+ตัวเลข เช่น `148e21ddb56789`

```bash
flyctl machine list
```

แล้วสั่งปิด (แทน `<ID>` ด้วยรหัสที่จดไว้)

```bash
flyctl machine stop <ID>
```

เช็คว่าปิดสนิท — ต้องขึ้นสถานะ `stopped`

```bash
flyctl status
```

✅ **ติดตั้งเสร็จ** ตอนนี้มีตัวสำรองพร้อมใช้ นอนรออยู่ ไม่กินเงิน ไม่ชนกับ Render

---

# 🚨 เวลา Render ล่ม — วิธีสลับมาใช้ตัวสำรอง

### 1. ปิด Render ก่อน (ห้ามข้าม!)
เข้า Render Dashboard → เลือก service `wire-mgmt` → เมนู **Settings** → **Suspend Web Service**

### 2. เปิดเครื่องสำรอง

```bash
cd "D:\Claude Code\wire-mgmt"
```

```bash
flyctl machine list
```

```bash
flyctl machine start <ID>
```

### 3. รอ ~1 นาที แล้วเช็คว่าขึ้นแล้ว

```bash
flyctl status
```

ถ้ามีปัญหา ดู log ได้ที่

```bash
flyctl logs
```

### 4. เข้าใช้งานที่ `https://wire-mgmt.fly.dev`
ข้อมูลจะต่อจาก Render ทุกอย่าง เพราะใช้ฐานข้อมูลก้อนเดียวกัน

---

# 🔙 เวลา Render กลับมาปกติ — สลับกลับ

### 1. ปิด Fly ก่อน (ห้ามข้าม!)

```bash
flyctl machine stop <ID>
```

```bash
flyctl status
```

ต้องเห็นสถานะ `stopped` ก่อนเท่านั้น ถึงไปขั้นต่อไปได้

### 2. เปิด Render กลับ
Render Dashboard → **Resume Web Service** → แล้วกด **Manual Deploy → Deploy latest commit**

งานที่กรอกตอนใช้ Fly อยู่ในฐานข้อมูลกลางแล้ว Render จะโหลดต่อได้เลย ไม่ต้องย้ายข้อมูลเอง

---

# 🔧 อัปเดตโค้ดใหม่ขึ้นตัวสำรอง

ตัวสำรอง**ไม่ได้ deploy อัตโนมัติตาม git** เหมือน Render ต้องสั่งเอง
(ทำตอน Fly ปิดอยู่ได้เลย ปลอดภัย — deploy เสร็จมันจะไม่เปิดเอง)

```bash
cd "D:\Claude Code\wire-mgmt"
```

```bash
flyctl deploy --remote-only --ha=false
```

แนะนำให้ทำทุกครั้งหลังมีการแก้ระบบใหญ่ๆ ไม่งั้นวันที่ต้องใช้จริงจะได้เวอร์ชันเก่า

---

# ❓ ปัญหาที่อาจเจอ

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| `ไม่รู้จักคำสั่ง flyctl` | ใช้หน้าต่างเก่า → ปิดแล้วเปิด PowerShell ใหม่ |
| `Name has already been taken` | ชื่อแอปซ้ำกับคนอื่น → ตั้งชื่อใหม่ + แก้บรรทัดแรกใน `fly.toml` |
| deploy ค้างนาน / ขึ้น error ตอน build | ปกติครั้งแรกนาน 10–20 นาที · ถ้า error จริงให้ส่ง log มาให้ดู |
| ต้องผูกบัตรก่อน | เป็นข้อบังคับของ Fly.io เอง ข้ามไม่ได้ |
| ใน log ขึ้น `🛑 หยุดบันทึกลงฐานข้อมูลกลาง` | **เปิดพร้อมกัน 2 ที่อยู่** → ปิดฝั่งที่ไม่ได้ใช้ทันที แล้วเช็กว่างานที่เพิ่งกรอกฝั่งนั้นครบไหม |

---

# 💡 ถ้าคิดว่าขั้นตอนนี้ยุ่งยากเกินไป

มีทางเลือกที่ไม่ต้องใช้คำสั่งเลย ทำผ่านหน้าเว็บอย่างเดียว เช่น **Koyeb** หรือ **Railway**
(กดเชื่อม GitHub → เลือก repo → ใส่ DATABASE_URL → กด Deploy · เปิด/ปิดด้วยปุ่มในหน้าเว็บ)
บอกผมได้ถ้าอยากเปลี่ยนไปทางนั้น เดี๋ยวเตรียมให้ใหม่
